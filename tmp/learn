一致性哈希算法
    一种特殊的哈希算法
    目的：用于解决分布式系统下的数据分布问题
    原理：将所有的节点哈希到一个环形空间中，每一个节点负责环上上一个节点到自己节点之间的区域（顺时针下）。这样做的目的就是为了保证数据的均匀分布，避免出现数据过于集中的情况，从而提高整体的处理效率。
    优点：1.平衡性
         2.单调性
         3.分散性
         4.灵活性

    缺点：1.不完全平衡性
         2.复杂性
